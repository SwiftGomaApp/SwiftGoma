const PLAN_SLUGS = {
  STARTER: "starter",
  BUSINESS: "business",
  ENTERPRISE: "enterprise",
};

const BILLING_CYCLES = {
  MONTHLY: "MONTHLY",
  YEARLY: "YEARLY",
};

const SUPPORTED_CURRENCIES = ["USD", "CDF"];

module.exports = { PLAN_SLUGS, BILLING_CYCLES, SUPPORTED_CURRENCIES };
