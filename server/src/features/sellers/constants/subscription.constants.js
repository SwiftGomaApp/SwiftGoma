"use strict";

const OTP_TYPES = {
  SUBSCRIPTION_ACTION: "SUBSCRIPTION_ACTION",
};

const SUBSCRIPTION_ACTIONS = {
  SUBSCRIBE: "SUBSCRIBE",
  UPGRADE: "UPGRADE",
  RENEW: "RENEW",
};

const PENDING_ACTION_TTL_SECONDS = 15 * 60;
const DEPOSIT_CONTEXT_TTL_SECONDS = 24 * 60 * 60;

const REDIS_PREFIX = {
  PENDING_ACTION: "sub:pending_action",
  DEPOSIT_CONTEXT: "sub:deposit_ctx",
};

const BILLING_CYCLE_MONTHS = {
  MONTHLY: 1,
  YEARLY: 12,
};

const LIVE_STATUSES = ["ACTIVE", "PAST_DUE"];

const RATE_LIMITS = {
  READ: { windowMs: 60 * 1000, max: 30 },
  MUTATE: { windowMs: 5 * 60 * 1000, max: 8 },
};

module.exports = {
  OTP_TYPES,
  SUBSCRIPTION_ACTIONS,
  PENDING_ACTION_TTL_SECONDS,
  DEPOSIT_CONTEXT_TTL_SECONDS,
  REDIS_PREFIX,
  BILLING_CYCLE_MONTHS,
  LIVE_STATUSES,
  RATE_LIMITS,
};
